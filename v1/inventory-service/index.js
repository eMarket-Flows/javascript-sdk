const products = require('./products.api');
const categories = require('./categories.api');
const catalogues = require('./catalogues.api');

module.exports = {
    products,
    categories,
    catalogues
};